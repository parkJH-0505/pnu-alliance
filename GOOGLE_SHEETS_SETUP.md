# PNU Alliance - 구글 시트 기반 데이터 관리 가이드

## 개요
이 프로젝트는 **MySQL 데이터베이스 대신 구글 시트를 데이터 저장소로 사용**합니다. 전문 개발자가 아니어도 엑셀처럼 데이터를 관리할 수 있습니다.

---

## 1. 초기 설정

### 1.1 구글 시트 생성
1. [Google Sheets](https://sheets.google.com)에 접속
2. **새 스프레드시트 만들기**
3. 이름을 `PNU Alliance Data` 또는 원하는 이름으로 설정
4. 시트 URL에서 ID 복사 (예: `https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit`)
   - `SHEET_ID` = `1xa7YDW1kjvVr-oLwWZETjwhNAxJVFuXF1uXWLZoSXnk` (현재 사용 중)

### 1.2 서비스 계정 공유
1. 프로젝트 폴더의 `server/_core/google-sheets-key.json` 파일 확인
2. 파일 내 `client_email` 주소 확인 (예: `manus-pnu@gen-lang-client-0038868491.iam.gserviceaccount.com`)
3. 구글 시트에서 **공유** 클릭
4. 위의 이메일 주소를 **편집자** 권한으로 추가

### 1.3 코드 전환
`server/routers.ts`의 import 부분을 수정:

```typescript
// 기존
import { createApplication, getApplications, ... } from "./db";

// 변경
import { createApplication, getApplications, ... } from "./db-sheets";
```

---

## 2. 구글 시트 구조

앱이 시작되면 자동으로 다음 탭들이 생성됩니다:

### 2.1 Applications (합류 신청)
| 신청일시 | 이름 | 이메일 | 전화번호 | 졸업년도 | 전공 | 회사 | 직급 | 직군 | 동기 |
|:---|:---|:---|:---|:---|:---|:---|:---|:---|:---|
| 2025.01.15 10:30 | 김철수 | kim@email.com | 010-1234-5678 | 2020 | 컴퓨터공학 | 삼성 | 대리 | 개발 | 네트워킹 |

**관리 방법:**
- 신청이 들어오면 자동으로 행이 추가됩니다
- 상태 관리는 별도 열에서 수동으로 처리 (승인/거절 등)

### 2.2 Events (이벤트/모임)
| 회차 | 제목 | 날짜 | 장소 | 테마 | 설명 | 정원 | 생성일시 |
|:---|:---|:---|:---|:---|:---|:---|:---|
| 1 | 첫 번째 모임 | 2025.01.20 19:00 | 서울 강남 | 네트워킹 | 부산대 동문 모임 | 50 | 2025.01.10 |

**관리 방법:**
- 새로운 이벤트를 추가하려면 행을 추가하고 정보 입력
- 회차는 자동으로 증가하지 않으니 수동으로 설정
- 날짜 형식: `YYYY.MM.DD HH:MM`

### 2.3 EventRegistrations (이벤트 참가 신청)
| 신청일시 | 이벤트ID | 이름 | 회사 | 전화번호 | 이메일 | 신청자타입 |
|:---|:---|:---|:---|:---|:---|:---|
| 2025.01.15 14:22 | 1 | 박영희 | 네이버 | 010-9876-5432 | park@email.com | 신규참가자 |

**관리 방법:**
- 이벤트 참가 신청이 들어오면 자동으로 기록됨
- 신청자타입: `기존멤버` 또는 `신규참가자`

### 2.4 Members (멤버 명부)
| 이름 | 직급 | 회사 | 전화번호 | 소개 | 등급 | 생성일시 |
|:---|:---|:---|:---|:---|:---|:---|
| 박준홍 | 대표 | PNU Alliance | 010-1111-1111 | 커뮤니티 호스트 | launcher | 2025.01.01 |

**관리 방법:**
- 멤버를 추가/수정하려면 직접 행을 편집
- 등급: `ground-crew`, `launcher`, `rocket`, `orbiter`, `galaxy`, `cosmos`
- 새로운 멤버 추가 시 행을 삽입하고 정보 입력

### 2.5 News (뉴스/공지)
| 생성일시 | 타입 | 제목 | 내용 | 요약 | 작성자 | 발행일시 |
|:---|:---|:---|:---|:---|:---|:---|
| 2025.01.15 | UPDATE | 1월 정기 모임 개최 | 자세한 내용... | 1월 정기 모임을 개최합니다 | 박준홍 | 2025.01.15 |

**관리 방법:**
- 타입: `UPDATE`, `INTERVIEW`, `RECAP`
- 새로운 뉴스를 추가하려면 행을 삽입
- 발행일시: `YYYY.MM.DD` 형식

### 2.6 Inquiries (호스트 문의)
| 생성일시 | 이름 | 이메일 | 전화번호 | 내용 | 상태 |
|:---|:---|:---|:---|:---|:---|
| 2025.01.15 11:45 | 이순신 | lee@email.com | 010-5555-5555 | 참가 문의입니다 | pending |

**관리 방법:**
- 상태: `pending`, `answered`, `closed`
- 문의에 답변하면 상태를 `answered`로 변경

---

## 3. 일상적 운영 방법

### 3.1 새 이벤트 등록
1. 구글 시트 `Events` 탭 열기
2. 마지막 행 아래에 새 행 삽입
3. 회차, 제목, 날짜, 장소, 테마 등 입력
4. 저장하면 웹사이트에 자동으로 반영

### 3.2 멤버 정보 관리
1. `Members` 탭에서 멤버 추가/수정
2. 등급 변경 시 해당 행의 "등급" 열만 수정
3. 저장하면 웹사이트에 반영

### 3.3 뉴스/공지 작성
1. `News` 탭에서 새 행 추가
2. 타입(UPDATE/INTERVIEW/RECAP), 제목, 내용, 요약 입력
3. 저장하면 뉴스 섹션에 표시

### 3.4 참가 신청 확인
1. `EventRegistrations` 탭에서 이벤트별 참가자 확인
2. 참가자 정보를 다운로드하거나 직접 확인

---

## 4. 기술 세부사항

### 4.1 구글 시트 API 인증
- **인증 방식:** 서비스 계정 (Service Account)
- **파일:** `server/_core/google-sheets-key.json`
- **권한:** 스프레드시트 읽기/쓰기

### 4.2 데이터 동기화
- 시트에 데이터를 추가하면 **즉시 웹사이트에 반영**
- 웹사이트에서 신청/등록이 들어오면 **자동으로 시트에 기록**

### 4.3 코드 구조
```
server/_core/
├── googleSheetsDb.ts      # 구글 시트 CRUD 유틸리티
└── google-sheets-key.json # 서비스 계정 인증 파일

server/
└── db-sheets.ts           # MySQL 대체 데이터 접근 계층
```

---

## 5. 주의사항

### 5.1 헤더 행 수정 금지
- 각 탭의 첫 번째 행(헤더)은 **절대 수정하지 마세요**
- 헤더를 수정하면 데이터 읽기가 실패합니다

### 5.2 열 순서 유지
- 각 탭의 열 순서는 **변경하지 마세요**
- 새 열을 추가하려면 맨 뒤에 추가하세요

### 5.3 데이터 형식
- 날짜: `YYYY.MM.DD HH:MM` 형식 사용
- 숫자: 따옴표 없이 입력
- 텍스트: 자유롭게 입력 가능

### 5.4 공유 권한 관리
- 서비스 계정 이메일(`manus-pnu@...`)은 **항상 편집자 권한** 유지
- 다른 팀원 추가 시 원하는 권한 설정 가능

---

## 6. 문제 해결

### 6.1 데이터가 웹사이트에 반영되지 않음
- 구글 시트에서 저장했는지 확인
- 웹사이트를 새로고침 (F5)
- 서비스 계정 이메일이 시트에 공유되었는지 확인

### 6.2 신청/등록이 시트에 기록되지 않음
- 서비스 계정이 **편집자 권한**을 가지고 있는지 확인
- 시트 탭 이름이 정확한지 확인 (예: `Applications` 대소문자 구분)

### 6.3 특수 문자 입력 시 오류
- 큰따옴표("), 줄바꿈 등은 피하기
- 필요하면 다른 문자로 대체

---

## 7. 향후 확장

### 7.1 추가 기능
- 멤버 프로필 이미지 관리 (S3 연동)
- 자동 이메일 알림 (구글 시트 변경 시)
- 데이터 백업 (자동 또는 수동)

### 7.2 고급 관리
- 구글 시트 폼 연동 (자동 신청서 수집)
- 스크립트 자동화 (Apps Script)
- 데이터 분석 (피벗 테이블)

---

## 8. 지원

문제가 발생하면:
1. 이 가이드의 "문제 해결" 섹션 확인
2. 구글 시트 공유 권한 재확인
3. 개발자에게 문의

**현재 구글 시트 ID:** `1xa7YDW1kjvVr-oLwWZETjwhNAxJVFuXF1uXWLZoSXnk`
**서비스 계정:** `manus-pnu@gen-lang-client-0038868491.iam.gserviceaccount.com`
